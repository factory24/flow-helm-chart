apiVersion: v1
kind: Service
metadata:
  name: acme-challenge-responder
  namespace: istio-system
  labels:
    app: acme-challenge-responder
spec:
  selector:
    acme.cert-manager.io/http01-solver: "true"
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 8089
