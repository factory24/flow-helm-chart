{{ if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configMap.name }}
data:
    {{- range $k, $v := .Values.configMap.data }}
  {{ $k }}: {{ $v | quote }}
    {{- end }}
{{- end }}